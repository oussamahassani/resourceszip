<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
	<Properties> 
        <Property name="logdir">${sys:catalina.base}/logs</Property> 
        <Property name="layout">%style{%d{ISO8601}}{white} %highlight{%-5level }[%style{%t}{bright,blue}] %style{%C}{bright,yellow}: %msg%n%throwable</Property>
    </Properties> 
    <Appenders>
        <Console name="CONSOLE" target="SYSTEM_OUT">
            <PatternLayout pattern="${layout}" />
        </Console>

        <RollingFile name="CATALINA"
                     fileName="${logdir}/crmChifco.log"
                     filePattern="${logdir}/crmChifco-%d{dd-MMMM-yyyy}-%i.log.gz">
            <PatternLayout pattern="${layout}" />
            <Policies>
            <!-- rollover on startup, daily and when the file reaches 10 MegaBytes -->
                <!-- <OnStartupTriggeringPolicy /> -->
                <SizeBasedTriggeringPolicy size="10 MB" />
                <TimeBasedTriggeringPolicy />
            </Policies>
            <DefaultRolloverStrategy max="60" />
        </RollingFile>

    </Appenders>
    <Loggers>

        <Logger name="crm.chifco.com" level="info"></Logger>

        <Root level="info">
            <AppenderRef ref="CONSOLE" />
            <AppenderRef ref="CATALINA" />
        </Root>
    </Loggers>
</Configuration>
