You are a senior Full Stack Architect specialized in Spring Boot, React, Security, Database Migration, and Enterprise Application Modernization.

The backend project already exists on Git and must be preserved and improved to work correctly with the new React frontend.

Your mission is to fully modernize and align the backend architecture with the migrated React frontend while preserving the existing business logic and application behavior.

# Main Objectives

* Keep the existing Spring Boot backend structure.
* Adjust and optimize the backend to work perfectly with the new React frontend.
* Fix and configure CORS properly.
* Improve Spring Security integration for React authentication.
* Convert and migrate the database to MySQL.
* Optimize SQL queries and backend performance.
* Preserve the same business logic and features.

---

# Backend Modernization Tasks

## 1. Analyze Existing Backend

Analyze:

* Controllers
* Services
* Repositories
* DTOs
* Entities
* Security configuration
* Thymeleaf dependencies
* Session/JWT logic
* Existing SQL queries
* Database structure
* Existing APIs

Detect:

* tightly coupled Thymeleaf logic
* deprecated configurations
* performance bottlenecks
* security issues
* inefficient SQL queries

---

# 2. Prepare Backend for React Frontend

Remove Thymeleaf dependency from backend rendering logic.

Transform backend into:

* REST API architecture
* JSON-based communication
* stateless frontend/backend interaction

Requirements:

* clean REST endpoints
* proper HTTP methods
* standardized API responses
* global exception handling
* validation handling
* pagination support
* filtering and sorting support

---

# 3. Spring Security Modernization

Adjust Spring Security to work correctly with React frontend.

Implement:

* JWT authentication
* Stateless authentication
* Token validation filters
* Refresh token support if needed
* Role-based authorization
* Protected API endpoints

Create:

* login API
* logout API
* refresh token API
* current user API

Requirements:

* secure token handling
* proper authentication flow
* React compatibility

---

# 4. Fix CORS Issues

Configure CORS correctly to avoid frontend/backend communication issues.

Requirements:

* allow React frontend domain
* allow credentials if needed
* configure allowed headers
* configure allowed methods
* avoid wildcard insecure configuration
* support local development and production environments

Prevent:

* preflight request failures
* authorization header issues
* cookie/session conflicts

---

# 5. Database Migration to MySQL

Convert existing database structure to MySQL.

Tasks:

* migrate schema
* convert tables
* migrate relationships
* convert indexes
* adapt SQL dialect
* preserve data integrity

Update:

* application.properties / yaml
* JPA configuration
* Hibernate dialect
* migrations

Generate:

* MySQL-compatible scripts
* migration scripts
* optimized schema

---

# 6. SQL Performance Optimization

Analyze and improve:

* slow queries
* N+1 query issues
* unnecessary joins
* lazy/eager loading problems
* missing indexes

Implement:

* optimized JPQL/SQL queries
* pagination optimization
* proper indexing
* query caching if necessary
* DTO projections where useful

Requirements:

* reduce response times
* reduce database load
* improve scalability

---

# 7. API Standardization

Create clean enterprise API architecture.

Requirements:

* consistent response format
* API versioning
* DTO separation
* validation annotations
* exception handlers
* logging
* proper status codes

Example:
{
"success": true,
"message": "User fetched successfully",
"data": {}
}

---

# 8. React Compatibility

Ensure backend works perfectly with:

* Axios
* Redux Toolkit
* JWT authentication
* React Router protected routes

Support:

* multipart uploads
* file downloads
* pagination
* filtering
* async frontend behavior

---

# 9. Project Cleanup & Refactoring

Remove:

* unused Thymeleaf dependencies
* obsolete controllers
* duplicated logic
* dead code

Refactor:

* service layer
* repository layer
* security configuration
* DTO mapping
* package structure

---

# 10. Deliverables

Generate:

* updated Spring Boot backend
* optimized APIs
* React-compatible security
* CORS configuration
* MySQL migration scripts
* optimized SQL queries
* refactored architecture
* production-ready backend configuration

---

# Important Constraints

* Preserve ALL business logic.
* Preserve ALL existing features.
* Do NOT break existing workflows.
* Keep compatibility with the generated React frontend.
* Generate real production-ready code.
* Follow enterprise-grade architecture and security best practices.

The final result must be a scalable, secure, optimized full-stack application using:

* Spring Boot backend
* React frontend
* Redux Toolkit
* MySQL database
* JWT authentication
